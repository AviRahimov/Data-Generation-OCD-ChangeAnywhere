# Core
PyYAML>=6.0
Pillow>=10.0.0
numpy>=1.26,<2.4
scikit-image>=0.22.0
opencv-python-headless>=4.8.0
scipy>=1.11.4

# Deep learning
# NOTE: install CUDA wheels from the official index, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# transformers>=5 requires torch>=2.6 to load legacy .bin checkpoints
# (CVE-2025-32434). cu124 provides torch 2.6.0+cu124 for CPython 3.11.
torch>=2.6.0
torchvision>=0.21.0
transformers>=5.0.0
timm>=1.0.17

# SAM 3 (Meta Segment Anything Model 3)
sam3>=0.1.3
psutil

# Stable Diffusion 2 inpainting
diffusers>=0.30.0
accelerate>=1.0.0

# HuggingFace model hub (for checkpoint downloads)
huggingface-hub>=1.1.4
